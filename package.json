{
  "devDependencies": {
    "@types/node": "^18.15.0",
    "gts": "^3.1.1",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  }
}
